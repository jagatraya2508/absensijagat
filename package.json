{
    "name": "absensi-app",
    "version": "1.0.0",
    "description": "Aplikasi Absensi Karyawan dengan foto selfie dan GPS tracking",
    "scripts": {
        "dev": "concurrently -n backend,frontend \"npm run backend\" \"node scripts/wait-backend.js && npm run frontend\"",
        "backend": "cd backend && npm run dev",
        "frontend": "cd frontend && npm run dev",
        "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "dotenv": "^17.3.1",
        "marked": "^18.0.3"
    }
}
